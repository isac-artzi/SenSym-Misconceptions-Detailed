# Install everything with:
#     pip3 install -r requirements.txt

pandas>=2.0          # tables and CSV files
requests>=2.31       # sending prompts to the local Ollama server
matplotlib>=3.7      # charts
scikit-learn>=1.3    # cross-checking your confusion matrix and metrics
